{
  "name": "xr-messaging-root",
  "version": "1.0.0",
  "scripts": {
  "start": "node server.js",
  "build": "cd desktop-app && npm install && npm run build"
},
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
