{
  "name": "xr-messaging-root",
  "version": "1.0.0",
   "scripts": {
    "start": "npm-run-all --parallel start:server start:desktop",
    "start:server": "cd server && npm start",
    "start:desktop": "cd desktop-app && npm start"
   },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
